package common;

public class Progremmain {
	//Common : 공통으로 사용 클래스 (메소드,변수)
	//프로그램 메인 -> 멤버 -> 상품
public static void main(String[] args) {
	System.out.println("1.사용자 모드 2.관리자 모드 3.종료");
	productDTO [] tempDtos = new ProductDTO[common.items.length+1];
	productDTO [] tempDtos = new ProductDTO[common.items.length+1];
}
}
